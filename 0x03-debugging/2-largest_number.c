#include "main.h"

/**
* largest_number - returns largest 3 numbers
* @A: first integer
* @B: second integer
* Return: largest number
*/

int largest_number(int A, int B)
{
int largest;
if (A >= B)
{
largest = A;
}
else if (B >= A)
{
largest = B;
}
return (largest)
}
